// Draft consultation page — superseded by the dashboard booking flow.
// Kept as an empty stub so its route doesn't 404; intentionally renders nothing.
export default function ConsultationStub() {
  return null;
}
